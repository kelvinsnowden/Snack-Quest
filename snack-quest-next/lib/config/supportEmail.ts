/**
 * The one support email every customer-facing surface points to,
 * mirroring `lib/config/whatsapp.ts`'s `WHATSAPP_CTA_NUMBER` — one
 * constant to edit rather than a string repeated at every call site.
 */
export const SUPPORT_EMAIL_ADDRESS = 'support@snackquests.shop';
