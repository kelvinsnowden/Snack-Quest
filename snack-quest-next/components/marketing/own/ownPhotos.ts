/**
 * The three real photos for `/own` (§ machine-owner lead-generation
 * landing page) — the machine in a real setting, the location-type
 * collage, and the Owner Portal dashboard. Named constants rather than
 * inline strings because `HERO_MACHINE_PHOTO_SRC` is reused across two
 * sections (the hero and the final CTA), same convention as
 * `BOX_HERO_SRC` on the homepage.
 */
export const HERO_MACHINE_PHOTO_SRC = '/own/hero-machine.webp';
export const LOCATIONS_COLLAGE_PHOTO_SRC = '/own/locations-collage.webp';
export const OWNER_PORTAL_PHOTO_SRC = '/own/owner-portal-dashboard.webp';
